/**
 * @deprecated Test-fixture compatibility export only.
 * Production code must build ORF state from database read models and must not import this module.
 */
export { initialOrfState } from "../testing/fixtures/initialOrfState";
