## 1. 测试目标

验证：未注册用户在注册页提交注册申请后进入待审核状态；管理员审核通过后，该用户可以再次使用注册邮箱和密码登录，并进入 ORF 已登录状态。

边界：

- 覆盖对象：注册成功、待审核、管理员通过、通过后再次登录。
- 不覆盖对象：注册表单校验、重复邮箱、拒绝申请、停用账号、错误密码。
- Setup 产物：独占测试管理员、未被占用的注册邮箱、注册表单页面。
- Clean 产物：邮箱为 "orf-register-e2e@orf.local" 的注册测试身份和用户、邮箱为 "orf-register-admin-e2e@orf.local" 的测试管理员身份和用户。
- 注册用户通过后的团队角色 应为 member。

## 2. 状态模型

- B：基准状态。
  - [api] 前端服务 应可用。
  - [api] 后端服务 应可用。
  - [api] 前端登录页入口 应可访问。
  - [api] 当前会话查询接口 应可访问。
  - [prisma] ORF 数据库 应可连接。
  - [prisma] ORF 数据库 schema 应为 当前测试版本。
  - [api] Ory/Kratos Admin/Public API 应可访问。
  - [api] 当前会话 应为 未登录。
  - [playwright] 当前浏览器 应不存在 Ory session cookie。
  - [playwright] 当前浏览器 storage 应不包含 登录态。

- Setup：构造 S0。
  - [api] 撤销注册测试登录身份可能残留的 Ory session。
  - [api] 删除邮箱为 `orf-register-e2e@orf.local` 的注册测试登录身份。
  - [prisma] 删除注册测试用户的默认团队成员关系。
  - [prisma] 删除邮箱为 `orf-register-e2e@orf.local` 的注册测试用户。
  - [api] 准备邮箱为 `orf-register-admin-e2e@orf.local` 的测试管理员登录身份，并设置固定测试密码。
  - [prisma] 准备邮箱为 `orf-register-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的测试管理员用户。
  - [api] 撤销测试管理员登录身份可能残留的 Ory session。
  - [playwright] 清理浏览器状态。
  - [playwright] 打开 登录页。
  - [playwright] 点击登录页的 `Register` 切换到注册模式。

- S0：Action 前状态。
  - [playwright] 当前页面 应为 注册页。
  - [playwright] 注册页标题 "Register" 应可见。
  - [playwright] 姓名输入框 应可见。
  - [playwright] 姓名输入框的值 应为空。
  - [playwright] 邮箱输入框 应可见。
  - [playwright] 邮箱输入框的值 应为空。
  - [playwright] 密码输入框 应可见。
  - [playwright] 密码输入框的值 应为空。
  - [playwright] "Create Account" 注册操作 应可见。
  - [playwright] "Create Account" 注册操作 应可点击。
  - [api] 当前会话 应为 未登录。
  - [api] 认证系统中 应存在 邮箱为 `orf-register-admin-e2e@orf.local` 的测试管理员登录身份。
  - [api] 测试管理员登录身份 的密码凭据 应可用。
  - [prisma] ORF 业务系统中 应存在 邮箱为 `orf-register-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的测试管理员用户。
  - [api] 认证系统中 应不存在 注册测试登录身份。
  - [prisma] ORF 业务系统中 应不存在 注册测试用户。

- Action：被测业务流程。
  - [playwright] 在姓名输入框输入 `ORF Register E2E`。
  - [playwright] 在邮箱输入框输入 `orf-register-e2e@orf.local`。
  - [playwright] 在密码输入框输入固定测试密码。
  - [playwright] 在点击 "Create Account" 注册操作前注册注册申请接口响应捕获。
  - [playwright] 点击 "Create Account" 注册操作。
  - [playwright] "等待注册审核" 状态 应可见。
  - [api] 记录 注册响应用户。
  - [prisma] 记录 注册测试用户的待审核状态。
  - [playwright] 点击 `退出登录` 清理注册用户当前会话。
  - [playwright] 清理注册用户浏览器状态。
  - [playwright] 打开 登录页。
  - [playwright] 在邮箱输入框输入测试管理员邮箱。
  - [playwright] 在密码输入框输入测试管理员密码。
  - [playwright] 点击 `Sign In` 登录操作。
  - [api] 当前会话 应为 测试管理员已登录。
  - [playwright] 打开 成员管理页面。
  - [playwright] 成员管理列表 应显示 `ORF Register E2E` 或 `orf-register-e2e@orf.local`。
  - [playwright] 在点击 `通过` 操作前注册审批通过接口响应捕获。
  - [playwright] 点击该用户行的 `通过` 操作。
  - [api] 审批通过接口响应 应成功。
  - [prisma] ORF 业务系统中 注册测试用户状态 应变为 `active`，且成员关系角色 应仍为 `member`。
  - [playwright] 管理员退出登录。
  - [playwright] 清理管理员浏览器状态。
  - [playwright] 打开 登录页。
  - [playwright] 在邮箱输入框输入注册测试用户邮箱。
  - [playwright] 在密码输入框输入注册测试用户密码。
  - [playwright] 点击 `Sign In` 登录操作。

- S1：Action 后状态。
  - [api] 注册申请接口响应 应成功。
  - [api] 注册响应的用户邮箱 应匹配 测试邮箱。
  - [api] 注册响应的用户状态 应为 `pending`。
  - [api] 注册响应的用户角色 应为 `member`。
  - [playwright] "等待注册审核" 提示 应曾可见。
  - [prisma] ORF 业务系统中 应存在 注册测试用户。
  - [prisma] 注册测试用户 的状态 应曾为 `pending`。
  - [prisma] 注册测试用户 的成员关系角色 应为 `member`。
  - [api] 注册测试用户再次登录后的当前会话 应为 邮箱为 `orf-register-e2e@orf.local`、角色为 `member`、状态为 `active` 的已登录会话。
  - [playwright] 再次登录后的当前页面 应为 ORF 已登录页面。
  - [playwright] 主导航 应可见。
  - [playwright] 当前用户入口 应可见。
  - [playwright] 成员管理入口 应不对该普通成员可见。

- Clean：恢复 B。
  - [api] 撤销当前会话的 Ory session。
  - [playwright] 当前页面离开 ORF 前端应用。
  - [playwright] 清理浏览器状态。
  - [api] 撤销注册测试登录身份的残留 Ory session。
  - [api] 删除邮箱为 `orf-register-e2e@orf.local` 的注册测试登录身份。
  - [prisma] 删除注册测试用户的默认团队成员关系。
  - [prisma] 删除邮箱为 `orf-register-e2e@orf.local` 的注册测试用户。
  - [api] 撤销测试管理员登录身份的残留 Ory session。
  - [api] 删除邮箱为 `orf-register-admin-e2e@orf.local` 的测试管理员登录身份。
  - [prisma] 删除测试管理员的默认团队成员关系。
  - [prisma] 删除邮箱为 `orf-register-admin-e2e@orf.local` 的测试管理员用户。
