# 成员管理页面基础元素展示

## 1. 测试目标

验证：系统管理-成员管理页面基础元素应正常展示，包括搜索框、角色筛选、用户列表、新增用户按钮、用户统计和角色统计。

## 2. 状态-动作模型

- B：基准状态。
  - [B-1] [api] 前端服务 应可用。
  - [B-2] [api] 后端服务 应可用。
  - [B-3] [api] 前端登录页入口 应可访问。
  - [B-4] [api] 当前会话查询能力 应可用。
  - [B-5] [prisma] ORF 数据库 应可连接。
  - [B-6] [prisma] ORF 数据库 schema 应为 当前测试版本。
  - [B-7] [api] Ory/Kratos 认证服务的管理和公共访问能力 应可用。
  - [B-8] [api] 当前会话 应为 未登录。
  - [B-9] [playwright] 当前浏览器 应不存在 Ory 登录会话 cookie。
  - [B-10] [playwright] 当前浏览器 应不保留本地登录态。

- Setup：构造 S0。
  - [Setup-1] [api] 准备邮箱为 `orf-system-members-overview-admin-e2e@orf.local`、使用固定测试密码的成员管理页面管理员测试认证身份。
  - [Setup-2] [prisma] 准备邮箱为 `orf-system-members-overview-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的成员管理页面管理员测试用户。
  - [Setup-3] [api] 设置成员管理页面管理员测试用户默认进入页面为 首页（默认系统页）。
  - [Setup-4] [prisma] 准备姓名为 "ORF Members Overview Member E2E"、邮箱为 `orf-system-members-overview-member-e2e@orf.local`、角色为 `member`、状态为 `active` 的成员管理页面普通成员测试用户。
  - [Setup-5] [api] 撤销邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试认证身份的残留登录会话。
  - [Setup-6] [playwright] 移除当前浏览器中的残留登录态。
  - [Setup-7] [playwright] 打开 ORF 登录页。
  - [Setup-8] [playwright] 在邮箱输入框输入 `orf-system-members-overview-admin-e2e@orf.local`。
  - [Setup-9] [playwright] 在密码输入框输入成员管理页面管理员测试固定密码。
  - [Setup-10] [playwright] 点击 "Sign In" 登录操作。
  - [Setup-11] [api] 当前会话 应为 邮箱为 `orf-system-members-overview-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的已登录会话。
  - [Setup-12] [playwright] 打开 成员管理页面。

- S0：Action 前状态。
  - [S0-1] [api] 当前会话 应为 邮箱为 `orf-system-members-overview-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的已登录会话。
  - [S0-2] [playwright] 当前页面 应为 成员管理页面。
  - [S0-3] [playwright] 成员管理页面标题 应可见。
  - [S0-4] [prisma] 邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试用户 应存在。
  - [S0-5] [prisma] 邮箱为 `orf-system-members-overview-member-e2e@orf.local` 的成员管理页面普通成员测试用户 应存在。

- Action：被测业务动作。
  - [Action-1] [playwright] 查看 成员管理页面。

- S1：Action 后状态。
  - [S1-1] [playwright] 成员管理页面的搜索框 应可见。
  - [S1-2] [playwright] 成员管理页面的搜索框占位文案 应为 "搜索姓名或邮箱"。
  - [S1-3] [playwright] 成员管理页面的角色筛选 应可见。
  - [S1-4] [playwright] 成员管理页面的角色筛选 应包含 "全部角色" 选项。
  - [S1-5] [playwright] 成员管理页面的角色筛选 应包含 "管理员" 选项。
  - [S1-6] [playwright] 成员管理页面的角色筛选 应包含 "成员" 选项。
  - [S1-7] [playwright] 成员管理页面的用户列表 应可见。
  - [S1-8] [playwright] 成员管理页面的用户列表 应显示 管理员测试用户。
  - [S1-9] [playwright] 成员管理页面的用户列表 应显示 普通成员测试用户。
  - [S1-10] [playwright] 成员管理页面的新增用户按钮 应可见。
  - [S1-11] [playwright] 成员管理页面的新增用户按钮 应可点击。
  - [S1-12] [playwright] 成员管理页面的用户统计 应可见。
  - [S1-13] [playwright] 成员管理页面的用户统计数 应大于等于 2。
  - [S1-14] [playwright] 成员管理页面的角色统计 应可见。
  - [S1-15] [playwright] 成员管理页面的角色统计数 应大于等于 2。
  - [S1-16] [api] 当前会话 应仍为 邮箱为 `orf-system-members-overview-admin-e2e@orf.local`、角色为 `admin`、状态为 `active` 的已登录会话。

- Clean：恢复 B。
  - [Clean-1] [api] 注销当前登录会话。
  - [Clean-2] [playwright] 离开当前 ORF 前端页面。
  - [Clean-3] [playwright] 移除当前浏览器中的残留登录态。
  - [Clean-4] [api] 撤销邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试认证身份的残留登录会话。
  - [Clean-5] [api] 删除邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试认证身份。
  - [Clean-6] [api] 恢复邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的默认进入页面为 系统默认。
  - [Clean-7] [prisma] 删除邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试用户的默认团队成员关系。
  - [Clean-8] [prisma] 删除邮箱为 `orf-system-members-overview-member-e2e@orf.local` 的成员管理页面普通成员测试用户的默认团队成员关系。
  - [Clean-9] [prisma] 删除邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试用户。
  - [Clean-10] [prisma] 删除邮箱为 `orf-system-members-overview-member-e2e@orf.local` 的成员管理页面普通成员测试用户。
  - [Clean-11] [api] 邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试认证身份 应不存在。
  - [Clean-12] [prisma] 邮箱为 `orf-system-members-overview-admin-e2e@orf.local` 的成员管理页面管理员测试用户 应不存在。
  - [Clean-13] [prisma] 邮箱为 `orf-system-members-overview-member-e2e@orf.local` 的成员管理页面普通成员测试用户 应不存在。
