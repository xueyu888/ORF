# Fantasy Task UI Experiments

This folder keeps standalone Fantasy Task UI prototype packages out of the repository root.

| Folder | Source |
| --- | --- |
| `v1` | Former `fantasy-task-ui` |
| `v2` | Former `fantasy-task-ui-v2` |
| `v3` | Former `fantasy-task-ui-v3` |
| `v4` | Former `fantasy-task-ui-v4` |
| `v5` | Former `fantasy-task-ui-v5` |
| `v6` | Former `fantasy-task-ui-v6` |
| `v7` | Former `fantasy-task-ui-v7` |

Each version is still an independent package. Run commands from the version folder, for example:

```bash
cd experiments/fantasy-task-ui/v7
npm install
npm run dev
```
