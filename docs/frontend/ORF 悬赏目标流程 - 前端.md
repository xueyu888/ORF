# ORF 悬赏目标流程 - 前端

## 页面定位

悬赏大厅只展示尚未被任何成员正式接受的悬赏目标。正式接受后，该目标进入我的挑战页面。

相关文档：

| 内容 | 文档 |
| --- | --- |
| 流程规则 | [ORF 悬赏目标流程设计.md](../rules/ORF%20悬赏目标流程设计.md) |
| 我的挑战 | [ORF 我的挑战页面 - 前端.md](./ORF%20我的挑战页面%20-%20前端.md) |
| 战利品提交 | [ORF 提交战利品页 - 前端.md](./ORF%20提交战利品页%20-%20前端.md) |
| 积分展示 | [积分结算规则 - 前端.md](./积分结算规则%20-%20前端.md) |

## 页面内容

| 区域 | 必要内容 |
| --- | --- |
| 页头 | `悬赏大厅`、当前周期、当前用户积分、积分明细入口、我的挑战入口、提出候选悬赏指标入口 |
| 征召令 | 当前用户被征召的目标 |
| 工具栏 | 搜索、难度筛选、目标筛选、排序 |
| 可申请目标 | 目标卡片列表 |
| 轻详情 | 目标说明、指标清单、当前操作 |

## 目标卡片

目标卡片展示：

- 目标标题。
- 目标说明。
- 悬赏指标清单。
- 目标总分。
- 最高难度。
- 剩余时间。
- `申请挑战` 操作。

卡片主体以 `Objective` 为主。

点击卡片主体打开轻详情；点击 `申请挑战` 打开确认弹窗。

## 征召卡片

征召卡片展示目标标题、目标说明、悬赏指标清单、目标总分、最高难度、剩余时间和操作。

当前阶段被征召成员必须接受挑战。接受后成为目标挑战者，并进入确认期；确认期内仍可发起重估。

## 状态

| 状态 | 前端展示 |
| --- | --- |
| 可申请 | 显示在可申请目标列表 |
| 申请中 | 当前用户已申请，等待指挥官确认 |
| 征召中 | 显示在征召令 |

确认期、执行中、待验收、已结算由我的挑战页面承接。

## 数据口径

| 字段 | 用途 |
| --- | --- |
| `Objective.challengers` | 当前目标挑战者；用于挑战者数量和互评范围 |
| `Objective.challengeApplications` | 当前用户申请状态 |
| `Objective.assignedChallengers` | 当前用户是否被征召 |
| `Objective.finalDueAt` | 剩余时间、排序、确认期计算 |
| `Result[]` | 目标下的悬赏指标清单、最高难度和目标总分来源 |

不在 `Result` 层表达挑战关系。

## 接口契约

悬赏大厅前端使用悬赏大厅专用接口读取列表数据。

| 接口 | 用途 |
| --- | --- |
| `GET /api/bounties` | 读取征召目标、可申请目标、筛选项和当前用户积分摘要 |
| `POST /api/objectives/:objectiveId/challenge-applications` | 申请挑战 |
| `PATCH /api/objectives/:objectiveId/challenge` | 接受征召或确认挑战 |

`GET /api/bounties` 的列表项以 `Objective` 为挑战对象，包含该目标下的 `Result[]` 作为悬赏指标清单。前端不从任务管理页大快照自行拼装悬赏大厅列表。

## 候选悬赏指标

候选悬赏指标的优先参与规则未定。当前不提供优先挑战入口，只保留 TODO。
