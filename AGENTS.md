# AGENTS.md

## 执行者边界

你应该做执行者，而不是替用户做产品或架构决策。

不确定时必须追问用户，不能强行替用户选择。只要当前修改涉及业务语义、架构取舍、旧逻辑去留、兼容策略、删除范围、权限边界、数据保留、安全边界、匿名性、加密链路或用户没有明确表达的产品规则，并且仓库事实不足以唯一确定答案，就必须停止写入修改，先列事实、冲突和选项，等待用户明确决策。

如果用户提出的要求与现有仓库事实、文档、代码、接口契约、状态流或测试存在自相矛盾，或者你无法判断应该选择哪一种方案，必须停止修改文档和代码。此时只允许做事实核查和问题陈述，不能继续替用户选择方向。

停止后必须把当前矛盾清楚列出来，说明：

- 用户要求是什么。
- 仓库当前事实是什么。
- 二者的冲突点是什么。
- 需要用户决策的选项有哪些。

只有用户明确做出决策后，才允许继续修改文档或代码。

## 旧代码与不确定决策边界

- 旧代码、旧文档、旧测试、旧接口、旧数据结构不得影响新业务。修改时发现旧实现与新业务契约冲突，应优先隔离或删除旧路径，避免让旧逻辑继续参与新业务状态流。
- 如果无法确定某段旧代码、旧接口、旧数据或兼容逻辑是否仍应保留，必须停止修改并追问用户，由用户决策删除、保留、迁移或兼容方案；不得自行做主。
- 任何涉及业务语义、安全边界、权限模型、数据保留、匿名性、加密链路、兼容策略、删除范围的判断，只要存在不确定性，都必须交给用户决策。
- 在用户决策前，只允许做事实核查、影响范围分析和选项说明，不得继续写入代码、文档、配置、测试或数据结构。

## 模块化与解耦原则

- 设计和实现应优先保持模块自我完备：模块内部应拥有清晰的状态、数据转换、边界校验、错误处理和测试入口，外部只通过显式接口或契约交互。
- 自我完备模块是优先设计单元。模块应在自己的边界内表达完整状态链、状态转换、不变量、输入输出契约、失败回退和测试入口；调用方只组合模块能力，不接管模块内部状态。
- 模块之间应通过显式接口组合，而不是通过共享隐式状态、页面局部变量或组件内部实现互相牵连。页面和组件优先承担编排、布局和交互入口职责，业务状态机、数据协调和契约校验应沉到独立模型、服务或适配层。
- 当一个流程天然存在多阶段状态，例如 `草稿 -> 提交中 -> 已提交待快照 -> 快照接管`，应优先建立命名清楚的状态模型和转换函数；不要把这些阶段拆散成多个布尔值、临时对象和副作用在页面里拼接。
- 抽象是否值得引入，优先看它是否能让模块边界更清楚、逻辑更自洽、组合更自然，而不是只看当前改动行数。一个小而完整的状态模型，通常优于分散在多个组件里的局部补丁。
- 跨模块共享能力时，先抽独立模型、服务或适配层，不把一个页面、组件或流程的内部实现泄漏给无关模块。
- 区分业务事实源、派生数据、展示状态和临时 UI 状态；需要跨层传递时必须说明事实源和职责边界。
- 新增抽象只有在能降低耦合、稳定契约或复用清晰业务能力时才引入；避免把多个模块绑成隐式状态机。

## 移动端适配原则

- ORF 现在需要把移动端作为一等使用场景适配；任何新增或修改页面、组件、样式、交互和前端状态时，都必须同时检查移动端视口下的结构、信息层级、触控操作和可读性。
- 旧有“移动端先不用管”“不要做移动端适配”之类局部历史约束不再作为默认规则。除非用户在当前任务里明确收窄范围，否则不能因为旧记忆或旧说明跳过移动端影响检查。
- 移动端适配应复用同一业务事实源、接口契约和权限模型；移动端状态只能是展示状态、导航状态或临时 UI 状态，不得另起一套业务事实或重复维护规则。
- 如果移动端行为涉及新的产品规则、信息取舍、权限边界、旧逻辑去留或兼容策略，并且仓库事实不足以唯一确定答案，必须先停止修改，列事实、冲突和选项，等待用户决策。

## 运行连续性原则

- ORF 的前端和后端服务应默认保持开启并处于可用状态；除非用户明确要求，否则不得停止、杀掉或清理任何 ORF 相关服务进程。
- 修改代码、配置、样式或依赖时可以接受短暂的编译失败、热更新失败或重启窗口，但不能让前端或后端长期处于不可用状态。
- 涉及数据库结构、迁移、schema guard、启动期必需字段或运行时代码会立即依赖的新表/新字段时，在迁移文件和代码契约达到自洽后，应优先执行迁移并恢复 ORF 基本可用，再继续做 UI、文档、测试或后续细节；不得把会导致后端无法启动的未迁移状态拖到整个任务最后。
- 数据库改动期间的目标是尽量缩短 ORF 业务整体中断时间。可以接受某个新功能、局部页面或正在改造的入口短暂不可用，但必须优先保证登录、主导航、核心页面和后端健康检查基本可用。
- 执行迁移前必须确认迁移方向不涉及未决的数据保留、删除、兼容或安全边界；若已明确可执行，应在迁移前后检查服务端口、健康检查和前端入口，发现 schema guard 或启动失败要立即先恢复运行。
- 如果修改需要重启服务，应优先执行“恢复运行”的闭环：确认当前启动方式和端口，完成必要重启，检查前端和后端入口重新可访问，再继续交付。
- 如果无法恢复到正常运行状态，必须立即停止继续扩大修改，说明当前前端、后端分别处于什么状态、已执行的恢复动作、阻塞原因和需要用户决策或介入的选项。

## 系统性修改触发规则

当用户要求“系统性 / 全面 / 彻底解决”时，不能把它理解为“多搜几个文件再打补丁”。必须先用 5-10 行写清：

- 完整业务状态链、状态转换和必须保持的不变量。
- 唯一事实源是什么；哪些只是派生数据、展示状态或临时 UI 状态。
- 是否存在隐式状态机、多事实源、跨层契约漂移或职责混杂。

如果根因是状态机不显式、事实源不清或契约不一致，先重建模型，再做代码、测试和文档的最小闭环修改。最终交付必须说明模型如何让职责更解耦、更正交。

## 提交与推送说明规则

创建提交前，必须先确认本次要提交的完整范围：

- 用 `git status -sb`、`git diff --stat`、`git diff --cached --stat` 明确工作区和暂存区。
- 分批提交时，每个提交只包含一个清晰功能职责，并用显式路径暂存，不能用 `git add .` 混入无关改动。
- 推送前必须用 `git log --reverse origin/<当前分支>..HEAD` 检查即将推送的完整提交列表；总结和检查必须覆盖这整个范围，不能只覆盖本轮刚拆出来的新提交。

创建提交时，提交说明必须是详细中文：

- 提交标题本身必须详细具体，因为 Git 图和很多工具默认只显示标题；标题要写清“改了什么”和“带来什么行为变化”。
- 不能只写英文、空泛标题或只翻译文件名；不能在中文说明里夹不必要的英文术语。
- 如果标题仍无法说清实际变化，必须使用多行提交说明，在正文补充核心影响面、用户可见变化或系统行为变化。
- 发现待推送范围里已有英文或过于空泛的本地未推送提交说明，必须先停止推送并改成中文；如果已经推送，必须先向用户说明只能后续补充或经用户明确同意后重写远端历史。

完成提交或推送后，必须用中文简要说明本次提交的实际内容，优先按提交粒度列出：

- `<提交短哈希>` + 一句话中文说明。
- 说明必须写清“改了什么”和“用户可感知或系统行为的变化”，不能只复述提交标题。
- 多个提交要按时间顺序或提交顺序排列，每条只描述一个清晰职责。

示例：

- `a48f192` 简化指标详情编辑：去掉显式保存/取消按钮，改为 Enter/失焦保存。
- `61942b4` 反馈系统通知：反馈创建、状态变化、反馈评论会通知相关人。
- `5db14e5` 我的挑战页新增“我的申请”待确认面板。
- `dc7f281` 悬赏/挑战里展示“待响应征召”的参与者。
- `43adf6c` 已被征召的人不能再重复申请同一个挑战。
- `d67fbeb` 新增项目删除：管理员可删项目，项目下目标移动到“未归属”。

如果用户关心影响面，还必须额外明确说明是否动了用户界面、数据库结构或数据写入行为，以及是否直接修改了 `testd`、`testd-doc`、数据化测试配置或用例。

## 数据化测试工作规则

修改 `testd-doc/cases`、`testd` 或数据化测试相关配置前，必须先阅读：

- `testd-doc/规范/数据化测试方法论.md`
- `testd-doc/规范/数据化测试步骤语言规范.md`
- `testd-doc/规范/数据化测试代码生成约束.md`
- 本次涉及的测试用例文档

新增用例、启用用例或标记为 `traceability: "verified"` 的用例，必须保证测试用例文档中的每条 `- [阶段-序号] [playwright|api|prisma|mock] 描述` 步骤和 `case.ts` 中的 `StepSpec` 严格一一对应，阶段、数量、顺序和步骤内容都必须一致。

每个 `StepSpec` 必须通过 `source.caseStepId` 和 `source.method` 回链到对应文档步骤；不得省略来源，也不得为没有文档步骤的执行动作虚构来源。

合并引入、尚未对齐的禁用用例可以暂时保留为 `traceability` 未验证状态，但不得启用；修改其业务步骤、operator 或 spec 前必须先对齐文档和 `case.ts` 并补齐 `source`。

不得为了普通功能修复或规则调整直接修改 `testd`、`testd-doc` 或数据化测试规范。除非用户明确要求维护数据化测试体系，否则只允许读取它们做事实核查。

新增 `tests/*.test.ts` 只能作为临时验证手段；验证完成后必须删除，不得作为交付内容保留。需要长期测试覆盖时，先向用户说明测试方案和影响范围，等待用户明确同意。
