# agents.md

## 兼容入口

本文件只保留小写路径兼容，不定义独立仓库规则。

| 事实源 | 用途 |
| --- | --- |
| `AGENTS.md` | 仓库协作规则、执行者边界、模块化原则、系统性修改规则、数据化测试规则 |
| `ORF.md` | ORF 产品思想和方法论 |
| `docs/README.md` | 仓库文档目录入口 |
| `docs/backend/` | 后端接口、字段契约、权限、业务计算 |
| `docs/frontend/` | 前端页面说明、UI 状态、可见交互 |

## 接口访问约定

前端数据统一通过同源 `/api` 请求后端接口；任务管理集合读取入口是 `/api/tasks-page`，写入接口以 `docs/backend/` 中的对应模块文档为准。
前端不直接写死后端域名或端口，开发和生产环境由代理或网关转发到实际后端服务。
