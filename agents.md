# ORF 仓库工作规则

## 接口访问约定

前端数据统一通过同源 `/api` 请求后端接口，例如 `/api/tasks`。
前端不直接写死后端域名或端口，开发和生产环境由代理或网关转发到实际后端服务。

本仓库用于建设名为 **ORF** 的项目管理工具。ORF 作为产品和项目管理工具，其产品理念、功能设计、业务流程和仓库协作方式必须遵循 `ORF.md` 中定义的 ORF 思想。

这里的“遵循 ORF”指的是产品和项目管理方法层面遵循 ORF，而不是要求每个模块、每个函数、每段代码都机械映射成 Objective、Results、Feedback。

ORF 的产品思想是：

- **Objective**：产品应帮助用户明确目标和方向。
- **Results**：产品应帮助用户把目标拆成可执行、可验证的结果。
- **Feedback**：产品应帮助用户通过反馈检查偏差，并支持后续调整。

## 强制工作流程

在修改产品需求、功能设计、业务文档或业务代码前，必须先完成产品层面的 ORF 合规检查：

1. 判断本次改动是否服务于 ORF 项目管理工具。
2. 判断本次改动是否支持目标、结果、反馈闭环中的至少一个关键能力。
3. 判断本次改动是否破坏 ORF 作为项目管理方法的核心思想。
4. 判断本次改动是否具备清晰的文档来源和验收方式。

只有通过以上检查，才允许继续修改仓库内容。

## 文档驱动代码规则

本仓库禁止通过 AI 会话直接要求修改项目管理软件的业务代码。所有业务代码的生成、修改、删除和重构，必须先有对应的仓库文档作为来源。

代码变更的唯一合法路径是：

```text
需求或问题 → 仓库文档 → 文档评审/澄清 → 根据文档生成或修改代码 → 验证 → 反馈修正文档或代码
```

这里的“仓库文档”可以是需求文档、产品设计、技术方案、接口说明、配置说明、验收标准或问题记录，不要求是 ORF 格式文档。

执行业务代码变更前，必须能指出对应的来源文档。来源文档至少要说明：

- 产品或工程目标：这次变更要解决什么问题。
- 预期结果：这次变更完成后应出现什么可验证结果。
- 反馈方式：如何发现实现偏差、使用问题或后续需要调整的地方。
- 验收方式：如何判断代码已经符合文档要求。

如果用户直接在 AI 会话中要求修改业务代码，但没有对应文档，必须拒绝修改代码，并提交以下错误：

```text
DOCUMENT_SOURCE_ERROR: 当前请求试图直接修改业务代码，但缺少对应的仓库文档来源，已拒绝修改代码。

请先补充或修改对应文档，明确目标、预期结果、反馈方式和验收方式后，再从文档生成或修改代码。
```

仓库配置、构建脚本、依赖清单和环境文件也应当由对应文档驱动。它们不需要机械套用 ORF 结构，但必须有清晰的目的、预期结果和验收方式。

任何影响产品、界面、业务流程、配置行为或代码实现的改动，都必须同步更新对应仓库文档。前端页面说明只描述 UI、UI 状态和用户可见交互；业务计算、权限判定、接口契约和数据持久化写入后端文档或规则文档。当前任务管理页面以前端文档 `docs/frontend/ORF 任务管理页面 - 前端.md` 和后端文档 `docs/backend/ORF 任务管理页面 - 后端.md` 为准。

## 拒绝规则

如果产品需求、功能设计或业务方案明显违背 ORF 产品思想，必须停止修改文档和代码，并提交以下错误：

```text
ORF_COMPLIANCE_ERROR: 当前请求不符合 ORF 产品思想，已拒绝修改文档和代码。

缺失或不清晰的部分：
- 产品目标:
- 预期结果:
- 反馈闭环:

请先补充清晰的产品目标、可验证的预期结果，以及可闭环的反馈机制。
```

以下情况必须拒绝修改：

- 只有任务动作，没有清晰产品目标。
- 只有愿望描述，没有可验证预期结果。
- 只有静态计划，没有反馈机制。
- 修改内容无法说明如何支撑 ORF 项目管理工具。
- 文档或代码会削弱 ORF 产品中目标、结果、反馈之间的闭环关系。
- 直接要求修改业务代码，但没有对应仓库文档来源。

## 文档规则

仓库文档必须服务于 ORF 项目管理工具。产品需求、功能设计、用户流程、业务规则等文档应尽量说明：

- 目标：本文档要解决的问题或定义的方向。
- 预期结果：读者完成阅读或执行后应得到的可验证结果。
- 反馈方式：本文档如何支持检查、复盘、调整或闭环。

技术说明、环境配置、依赖说明等文档不需要机械套用 ORF 标题，但必须写清楚目的、影响范围和验收方式。

### 前端文档规则

前端页面文档只记录 AI 改代码需要的组件定位、隐藏实现事实和当前实现限制。

组件树只表达稳定业务结构、父子关系和重复关系，不表达 DOM 包装结构、页面长相或浏览器里能直接看到的交互。

当前项目前端只面向桌面端 Web。页面设计、前端文档和验收标准暂不考虑手机端或移动端适配，除非用户明确更新该规则。

### 文档表述原则

产品、设计、流程和业务规则文档必须简洁，只写当前有效状态、必要字段、组件结构、业务规则和验收方式。

文档内容以支撑实现和验证为准，能省则省；不要写背景铺垫、过程说明、重复说明或可从界面直接看出的描述。

修改文档时，应直接把相关章节改写成当前规则，避免使用这类表述：

- “不再……”
- “原来……现在……”
- “之前……”
- “由 A 改成 B”
- “删除/取消某旧方案”

如果确实需要保留历史原因、迁移过程或决策背景，应单独写到变更记录、决策记录或复盘文档中。

禁止新增只描述口号、愿景或任务清单，但无法支撑产品决策、实现或反馈闭环的文档。

## 代码规则

代码不需要逐个模块、逐个函数套用 ORF 结构。代码层面的要求是：

- 业务代码变更必须来自对应来源文档，不能直接通过 AI 会话临时修改。
- 代码实现必须符合来源文档描述的产品能力、业务流程或工程约束。
- 业务代码必须遵循 `docs/project/code-standards.md` 中定义的代码规范。
- 代码不能引入与 ORF 产品思想冲突的项目管理范式或业务行为，除非先更新产品文档并说明关系。
- 新方案替换旧方案时，必须清理旧实现、旧入口、旧命名、旧样式和旧文档残留，保持仓库只有当前有效方案。
- 只有当前方案仍明确依赖的兼容层可以保留；保留时必须在来源文档写明原因、影响范围和退出条件。
- 代码变更必须配套必要的验证方式，例如测试、检查脚本、交互验证或明确的人工验收标准。

禁止新增没有文档来源、无法验收或会绕过文档驱动流程的业务代码、抽象和重构。

## 最低验收标准

每次涉及产品、文档、配置或代码的修改完成后，必须在最终说明中给出：

- 本次改动的目标。
- 本次改动产生的结果。
- 本次改动支持的反馈方式。
- 本次改动依据的来源文档。
- 已执行或无法执行的验证。

如果业务代码变更无法给出来源文档，必须停止修改代码。若产品需求或功能设计无法说明它如何服务 ORF 项目管理工具，必须拒绝该产品方向。

## Codex 活动播报

每轮 ORF 工作结束时，最终回复应提供活动播报字段，供自动 hook 发送到 Mattermost。字段必须由 AI 根据本轮上下文生成，不能由关键词模板拼接。

```text
播报问题：中文写用户这一轮真正的问题、质疑或要求，不照抄原话，但保留具体对象。
播报回答：英文总结 Codex 这一轮给出的回答、判断或完成结果。
播报语法：中文解释播报回答里一个真实出现的英文表达。
播报表情：贴合语境的梗图名。
```

`播报问题` 写用户的问题，`播报回答` 写 Codex 的回答。没有真实完成动作时，不能写成“已更新”“已修复”“已实现”。旧字段 `播报摘要`、`播报英文` 只用于兼容历史消息，新回复必须使用 `播报问题` 和 `播报回答`。
